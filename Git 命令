Git 是一个分布式版本控制系统，以下是一些常用的 Git 命令：

1. **git init:**
   - 初始化一个新的 Git 仓库。

2. **git clone \<repository>:**
   - 克隆一个远程仓库到本地。

3. **git add \<file>:**
   - 将文件添加到暂存区。

4. **git commit -m "message":**
   - 提交暂存区的文件，并附上提交信息。

5. **git status:**
   - 查看工作区和暂存区的状态。

6. **git pull:**
   - 从远程仓库拉取最新的修改。

7. **git push:**
   - 将本地修改推送到远程仓库。

8. **git branch:**
   - 列出所有分支。

9. **git checkout -b \<branch>:**
   - 创建并切换到新分支。

10. **git merge \<branch>:**
    - 合并指定分支到当前分支。

11. **git log:**
    - 查看提交日志。

12. **git remote -v:**
    - 查看远程仓库的信息。

13. **git fetch:**
    - 从远程仓库获取最新的分支，但不合并。

14. **git reset \<file>:**
    - 从暂存区撤销对文件的暂存。

15. **git diff:**
    - 查看工作区与暂存区的差异。

这些命令涵盖了 Git 的基本操作，可以帮助你进行版本控制和协同开发。请注意，Git 命令众多，上述只是一些常用命令的简要介绍。更详细的信息可以通过 `git --help` 或查阅 Git 文档获得。
